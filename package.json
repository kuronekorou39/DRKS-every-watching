{
  "name": "drks-every-watching",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20.6" },
  "scripts": {
    "fetch": "node --env-file=.env scripts/fetch.mjs",
    "sample": "node scripts/sample.mjs",
    "serve": "npx --yes http-server -c-1 -p 8080 .",
    "test": "node --test"
  }
}
